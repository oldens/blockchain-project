specVersion: "v1"
serviceId: "blockchain-project"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "blockchain-project-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
